PORT=8787
# Server/function scope only. Never prefix secrets with VITE_.
TYPESAFE_API_KEY=
TYPESAFE_MODEL=jev-latest
# Public inference kill switch. Per-IP throttling is NOT a global budget.
JEV_LIVE_ENABLED=true
